<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Template for the gitignored Secrets.plist.

  Copy this file to App/Secrets.plist and fill in client_secret, then regenerate
  the project (`xcodegen generate`). On first launch AppSecrets.load() seeds these
  values into the Keychain and reads from the Keychain thereafter.

  One client pair is shared by both the v3 and v4 realms (see ARCHITECTURE.md §13).
  NEVER commit a real client_secret — Secrets.plist is gitignored.
-->
<plist version="1.0">
<dict>
    <key>client_id</key>
    <string>ded3658e-53d1-478a-b957-9ffca6005c0e</string>
    <key>client_secret</key>
    <string>REPLACE_WITH_YOUR_POLAR_CLIENT_SECRET</string>
    <key>member_id</key>
    <string>yug-hercules-test</string>
</dict>
</plist>
